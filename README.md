#Automated Test

All test for freelance application with Hybrid Framework

#Running Tests
Clone the repository using

....bash
git clone https://github.com/gorrela/SepBatchFramework.git

git clone git@github.com:gorrela/SepBatchFramework.git

cd SepBatchFramework

To run the tests, run the following command

mvn clean test
